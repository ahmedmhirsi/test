<div class="{{ container_class|default('flex items-center') }}">
    <img src="/images/logo.png" 
         alt="SmartNexus Logo" 
         class="{{ logo_size|default('h-12 w-auto') }} group-hover:scale-105 transition-transform duration-300">
</div>
