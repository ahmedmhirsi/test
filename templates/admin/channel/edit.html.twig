{% extends 'backoffice.html.twig' %}

{% block title %}Modifier Channel - Admin{% endblock %}

{% block admin_content %}
<div class="max-w-3xl mx-auto">
    <div class="mb-6">
        <h1 class="text-3xl font-display font-black text-navy dark:text-white">Modifier le Channel</h1>
        <p class="text-gray-500 text-sm mt-1">Édition de : {{ channel.nom }}</p>
    </div>

    <div class="bg-white dark:bg-[#1e2235] rounded-xl shadow-sm border border-gray-100 dark:border-gray-800 p-6 md:p-8">
        {{ include('admin/channel/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
</div>
{% endblock %}
