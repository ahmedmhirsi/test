<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Rapport Analytics - {{ generated_at|date('d/m/Y') }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
            color: #333;
        }
        h1 {
            color: #1a1d2d;
            border-bottom: 4px solid #fbbf24;
            padding-bottom: 10px;
            margin-bottom: 30px;
        }
        h2 {
            color: #1a1d2d;
            border-bottom: 2px solid #3b82f6;
            padding-bottom: 8px;
            margin-top: 30px;
            margin-bottom: 15px;
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
            margin: 20px 0;
        }
        .stat-card {
            background: #f3f4f6;
            padding: 15px;
            border-left: 4px solid #3b82f6;
        }
        .stat-value {
            font-size: 28px;
            font-weight: bold;
            color: #1a1d2d;
        }
        .stat-label {
            font-size: 12px;
            color: #6b7280;
            margin-top: 5px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        th {
            background: #1a1d2d;
            color: white;
            padding: 12px;
            text-align: left;
            font-size: 12px;
        }
        td {
            padding: 10px 12px;
            border-bottom: 1px solid #e5e7eb;
            font-size: 11px;
        }
        tr:nth-child(even) {
            background: #f9fafb;
        }
        .badge {
            padding: 4px 8px;
            border-radius: 4px;
            font-weight: bold;
            font-size: 10px;
        }
        .badge-green { background: #d1fae5; color: #065f46; }
        .badge-yellow { background: #fef3c7; color: #92400e; }
        .badge-red { background: #fee2e2; color: #991b1b; }
        .badge-blue { background: #dbeafe; color: #1e40af; }
        .badge-purple { background: #ede9fe; color: #5b21b6; }
        .footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #e5e7eb;
            font-size: 10px;
            color: #6b7280;
            text-align: center;
        }
        .info-box {
            background: #eff6ff;
            border-left: 4px solid #3b82f6;
            padding: 15px;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <h1>Rapport Analytics & Collaboration</h1>
    
    <div class="info-box">
        <p><strong>Période analysée:</strong> {{ period }} jours</p>
        <p><strong>Date de génération:</strong> {{ generated_at|date('d/m/Y à H:i') }}</p>
    </div>

    <h2>Vue d'Ensemble</h2>
    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-value">{{ overall_stats.total_users }}</div>
            <div class="stat-label">Utilisateurs</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ overall_stats.total_meetings }}</div>
            <div class="stat-label">Meetings</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ overall_stats.total_channels }}</div>
            <div class="stat-label">Channels</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ overall_stats.total_polls }}</div>
            <div class="stat-label">Sondages</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ overall_stats.total_whiteboards }}</div>
            <div class="stat-label">Tableaux Blancs</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ overall_stats.active_meetings }}</div>
            <div class="stat-label">Meetings Actifs</div>
        </div>
    </div>

    <h2>Temps Moyen par Meeting</h2>
    <div class="stats-grid">
        <div class="stat-card">
            <div class="stat-value">{{ avg_duration.average_formatted }}</div>
            <div class="stat-label">Durée Moyenne</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ avg_duration.total_meetings }}</div>
            <div class="stat-label">Meetings Analysés</div>
        </div>
        <div class="stat-card">
            <div class="stat-value">{{ avg_duration.total_duration_formatted }}</div>
            <div class="stat-label">Temps Total</div>
        </div>
    </div>

    <h2>Taux de Participation par Utilisateur</h2>
    <table>
        <thead>
            <tr>
                <th>Utilisateur</th>
                <th style="text-align: center;">Meetings Participés</th>
                <th style="text-align: center;">Total Meetings</th>
                <th style="text-align: center;">Taux de Participation</th>
            </tr>
        </thead>
        <tbody>
            {% for user in participation_rates %}
                <tr>
                    <td>{{ user.name }}</td>
                    <td style="text-align: center;">{{ user.meetings_attended }}</td>
                    <td style="text-align: center;">{{ user.total_meetings }}</td>
                    <td style="text-align: center;">
                        <span class="badge {% if user.participation_rate >= 75 %}badge-green{% elseif user.participation_rate >= 50 %}badge-yellow{% else %}badge-red{% endif %}">
                            {{ user.participation_rate }}%
                        </span>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <h2>Productivité par Équipe</h2>
    <table>
        <thead>
            <tr>
                <th>Équipe</th>
                <th style="text-align: center;">Meetings Total</th>
                <th style="text-align: center;">Complétés</th>
                <th style="text-align: center;">Taux Complétion</th>
                <th style="text-align: center;">Sondages</th>
                <th style="text-align: center;">Tableaux</th>
                <th style="text-align: center;">Score Productivité</th>
            </tr>
        </thead>
        <tbody>
            {% for team in team_productivity %}
                <tr>
                    <td>{{ team.channel_name }}</td>
                    <td style="text-align: center;">{{ team.total_meetings }}</td>
                    <td style="text-align: center;">{{ team.completed_meetings }}</td>
                    <td style="text-align: center;">
                        <span class="badge badge-blue">{{ team.completion_rate }}%</span>
                    </td>
                    <td style="text-align: center;">{{ team.polls_created }}</td>
                    <td style="text-align: center;">{{ team.whiteboards_created }}</td>
                    <td style="text-align: center;">
                        <span class="badge badge-purple">{{ team.productivity_score }}</span>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        <p>Document généré le {{ generated_at|date('d/m/Y à H:i') }} - SmartNexusAI Collaboration Module</p>
        <p>Ce rapport contient des données confidentielles - Distribution restreinte</p>
    </div>
</body>
</html>
