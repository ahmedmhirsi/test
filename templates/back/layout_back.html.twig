{% extends 'base.html.twig' %}

{% block body %}
<div class="flex h-screen w-full flex-row overflow-hidden">
    {# Sidebar #}
    <aside class="w-64 bg-white dark:bg-slate-900 border-r border-slate-200 dark:border-slate-800 flex flex-col shrink-0">
        <div class="p-6 flex items-center gap-3">
            <div class="bg-primary/10 rounded-xl p-2">
                <span class="material-symbols-outlined text-primary text-3xl">support_agent</span>
            </div>
            <div class="flex flex-col">
                <h1 class="text-navy dark:text-white text-lg font-bold font-title leading-tight">Back Office</h1>
                <p class="text-navy/60 dark:text-slate-400 text-xs font-medium">Gestion Réclamations</p>
            </div>
        </div>
        
        <nav class="flex-1 flex flex-col gap-2 px-4 overflow-y-auto">
            <a class="flex items-center gap-3 px-3 py-3 rounded-lg {{ app.request.attributes.get('_route') starts with 'back_reclamation' ? 'bg-primary/10 text-primary' : 'text-navy/70 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-800 hover:text-primary' }} transition-colors group" href="{{ path('back_reclamation_index') }}">
                <span class="material-symbols-outlined {{ app.request.attributes.get('_route') starts with 'back_reclamation' ? 'fill-1' : '' }}">confirmation_number</span>
                <span class="text-sm {{ app.request.attributes.get('_route') starts with 'back_reclamation' ? 'font-bold' : 'font-medium' }}">Réclamations</span>
            </a>
        </nav>
        
        <div class="p-4 border-t border-slate-200 dark:border-slate-800">
            <div class="flex items-center gap-3">
                <div class="bg-primary rounded-full size-10 flex items-center justify-center text-white font-bold">
                    AD
                </div>
                <div class="flex flex-col">
                    <p class="text-sm font-bold text-navy dark:text-white">Administrateur</p>
                    <p class="text-xs text-navy/60 dark:text-slate-400">Admin</p>
                </div>
            </div>
        </div>
    </aside>

    {# Main Content #}
    <div class="flex-1 flex flex-col min-w-0 bg-background-light dark:bg-background-dark">
        {# Header #}
        <header class="h-16 bg-white dark:bg-slate-900 border-b border-slate-200 dark:border-slate-800 flex items-center justify-between px-6 shrink-0">
            <div class="flex items-center gap-4">
                <h2 class="text-xl font-bold font-title text-navy dark:text-white tracking-tight">
                    {% block page_title %}Gestion des Réclamations{% endblock %}
                </h2>
            </div>
        </header>

        {# Flash Messages #}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="mx-6 mt-4 p-4 rounded-lg {% if label == 'success' %}bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-300{% elseif label == 'error' %}bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-300{% else %}bg-blue-100 text-blue-700 dark:bg-blue-900/30 dark:text-blue-300{% endif %} flex items-center gap-2">
                    <span class="material-symbols-outlined">{{ label == 'success' ? 'check_circle' : 'info' }}</span>
                    <span>{{ message }}</span>
                </div>
            {% endfor %}
        {% endfor %}

        {# Main Content Area #}
        <main class="flex-1 overflow-y-auto p-6">
            {% block content %}{% endblock %}
        </main>
    </div>
</div>
{% endblock %}
