<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}SmartNexus AI - Back Office{% endblock %}</title>
         <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🔹</text></svg>">
        <script src="https://cdn.tailwindcss.com"></script>
        <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
        <style>
            body { font-family: 'Plus Jakarta Sans', sans-serif; }
            h1, h2, h3, h4, h5, h6, .font-display { font-family: 'Outfit', sans-serif; }
            .sidebar-link.active { background-color: #F1F5F9; color: #0F172A; font-weight: 600; }
        </style>
        {% block stylesheets %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </head>
    <body class="bg-white text-slate-800 h-screen flex overflow-hidden">
        <!-- Sidebar -->
        <aside class="w-72 bg-slate-900 border-r border-slate-800 flex flex-col flex-shrink-0 text-slate-400">
            <div class="p-6 mb-2">
                {{ include('_logo.html.twig', {
                    'variant': 'admin'
                }) }}
            </div>
            
            <nav class="flex-1 px-4 space-y-1">
                <div class="text-xs font-semibold text-slate-600 uppercase tracking-wider mb-3 mt-4 px-3">Général</div>
                
                <a href="{{ path('app_home') }}" class="sidebar-link flex items-center gap-3 px-3 py-2.5 hover:bg-slate-800 hover:text-white rounded-lg transition-all {% if app.request.get('_route') == 'app_home' %}bg-slate-800 text-white{% endif %}">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"></path></svg>
                    Dashboard
                </a>
                
                <div class="text-xs font-semibold text-slate-600 uppercase tracking-wider mb-3 mt-6 px-3">Recrutement</div>
                
                <a href="{{ path('app_offre_emploi_index') }}" class="sidebar-link flex items-center gap-3 px-3 py-2.5 hover:bg-slate-800 hover:text-white rounded-lg transition-all {% if 'offre' in app.request.get('_route') %}bg-slate-800 text-white{% endif %}">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                    Offres d'Emploi
                </a>
                <a href="{{ path('app_candidature_index') }}" class="sidebar-link flex items-center gap-3 px-3 py-2.5 hover:bg-slate-800 hover:text-white rounded-lg transition-all {% if 'candidature' in app.request.get('_route') %}bg-slate-800 text-white{% endif %}">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path></svg>
                    Candidatures
                </a>
                
                <div class="text-xs font-semibold text-slate-600 uppercase tracking-wider mb-3 mt-6 px-3">Formation</div>
                
                 <a href="{{ path('app_formation_index') }}" class="sidebar-link flex items-center gap-3 px-3 py-2.5 hover:bg-slate-800 hover:text-white rounded-lg transition-all {% if 'formation' in app.request.get('_route') %}bg-slate-800 text-white{% endif %}">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
                    Formations
                </a>
            </nav>
            
            <div class="p-4 border-t border-slate-800">
                <!-- User section - will be integrated with teammate's User entity -->
                <div class="flex items-center gap-3 p-2">
                    <div class="w-8 h-8 rounded-full bg-gradient-to-br from-brand-500 to-indigo-600 flex items-center justify-center text-white text-sm font-bold ring-2 ring-slate-800">
                        A
                    </div>
                    <div class="flex-1 min-w-0">
                        <div class="text-sm font-semibold text-slate-200 truncate">Admin</div>
                        <div class="text-[11px] text-slate-500">Recrutement</div>
                    </div>
                </div>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="flex-1 flex flex-col h-full overflow-hidden bg-[#F1F5F9]">
            <!-- Header -->
            <header class="bg-white/80 backdrop-blur-sm border-b border-slate-200 h-16 flex items-center justify-between px-8 sticky top-0 z-10">
                <div class="flex items-center gap-3 text-sm">
                    <span class="text-slate-400">Back Office</span>
                    <span class="text-slate-300">/</span>
                    <span class="font-medium text-slate-900">Administration</span>
                </div>
                <!-- ... header items ... -->
            </header>

            <!-- Scrollable Page Content -->
            <div class="flex-1 overflow-y-auto px-8 py-8">
                {% for message in app.flashes('success') %}
                    <div class="mb-6 p-4 rounded-xl bg-emerald-50 text-emerald-700 border border-emerald-100 flex items-center gap-3 shadow-sm">
                         <div class="w-8 h-8 bg-emerald-100 rounded-full flex items-center justify-center shrink-0">
                            <svg class="w-5 h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                         </div>
                        <span class="font-medium">{{ message }}</span>
                    </div>
                {% endfor %}
                
                {% block body %}{% endblock %}
            </div>
        </main>
    </body>
</html>
