<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Channels</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; }
        h1 { text-align: center; color: #1e3a5f; margin-bottom: 5px; }
        .date { text-align: center; color: #666; margin-bottom: 20px; font-size: 10px; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background-color: #1e3a5f; color: white; padding: 8px; text-align: left; font-size: 11px; }
        td { padding: 6px 8px; border-bottom: 1px solid #ddd; font-size: 11px; }
        tr:nth-child(even) { background-color: #f9f9f9; }
    </style>
</head>
<body>
    <h1>📢 Liste des Channels</h1>
    <p class="date">Généré le {{ "now"|date("d/m/Y H:i") }}</p>
    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Nom</th>
                <th>Type</th>
                <th>Description</th>
                <th>Statut</th>
                <th>Max Participants</th>
            </tr>
        </thead>
        <tbody>
            {% for channel in channels %}
            <tr>
                <td>{{ channel.id }}</td>
                <td>{{ channel.nom }}</td>
                <td>{{ channel.type }}</td>
                <td>{{ channel.description|default('-')|slice(0, 80) }}</td>
                <td>{{ channel.statut }}</td>
                <td>{{ channel.maxParticipants ?? '-' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    <p style="margin-top: 20px; font-size: 10px; color: #999;">Total: {{ channels|length }} channel(s)</p>
</body>
</html>
