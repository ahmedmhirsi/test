{% extends 'base.html.twig' %}

{% block title %}Dashboard - SmartNexus AI{% endblock %}

{% block body %}
<div class="min-h-screen bg-gradient-to-br from-background-light to-white flex items-center justify-center p-6">
    <div class="bg-white rounded-2xl shadow-xl border border-[#e7e3da] p-8 md:p-12 max-w-lg w-full text-center">
        <div class="size-20 mx-auto mb-6 rounded-full bg-gradient-to-br from-primary to-yellow-400 flex items-center justify-center">
            <span class="material-symbols-outlined text-navy text-4xl">waving_hand</span>
        </div>
        
        <h1 class="text-3xl font-bold text-navy mb-4">Bienvenue, {{ app.user.prenom }} !</h1>
        
        <p class="text-gray-600 mb-8">
            Votre compte est bien configuré. Vous serez redirigé vers votre espace personnel dès que votre rôle sera défini par un administrateur.
        </p>
        
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
            <div class="flex items-center gap-3 text-blue-700">
                <span class="material-symbols-outlined">info</span>
                <span class="text-sm font-medium">Votre rôle actuel : <strong>{{ app.user.roles|first }}</strong></span>
            </div>
        </div>
        
        <div class="flex flex-col gap-3">
            <a href="{{ path('app_home') }}" class="w-full px-6 py-3 rounded-lg bg-primary hover:bg-yellow-400 text-navy font-bold transition-all">
                Retour à l'accueil
            </a>
            <a href="{{ path('app_logout') }}" class="w-full px-6 py-3 rounded-lg border border-gray-300 hover:border-red-400 hover:text-red-600 text-gray-700 font-medium transition-all">
                Se déconnecter
            </a>
        </div>
    </div>
</div>
{% endblock %}
