{{ form_start(form, {'attr': {'class': 'space-y-6', 'novalidate': 'novalidate'}}) }}
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="md:col-span-2">
            {{ form_row(form.titre, {'attr': {'placeholder': 'Ex: Livraison Jalon Beta-01'}}) }}
        </div>

        <div class="md:col-span-2">
            {{ form_row(form.description, {'attr': {'placeholder': 'Définition de l\'étape stratégique...'}}) }}
        </div>

        {{ form_row(form.dateEcheance) }}
        {{ form_row(form.priorite) }}
        
        <div class="md:col-span-2">
            {{ form_row(form.statut) }}
        </div>

        {{ form_row(form.projet) }}
        {{ form_row(form.sprint) }}
    </div>

    <div class="pt-8 border-t border-white/5 flex justify-end gap-4">
        <a href="{{ path('app_jalon_index') }}" class="px-6 py-4 rounded-xl text-xs font-black uppercase text-gray-500 hover:text-white transition-colors">
            Neutraliser
        </a>
        <button class="btn-nexus-primary rounded-xl px-10 py-4 text-sm shadow-xl shadow-nexus-primary/20 group">
             <span class="material-symbols-outlined text-[20px] group-hover:rotate-12 transition-transform">flag</span>
             <span class="ml-2">{{ button_label|default('Établir') }}</span>
        </button>
    </div>

{{ form_end(form) }}
