{% extends user_layout %}

{% block title %}Nouveau Jalon – SmartNexus AI{% endblock %}

{% block body %}
    {# ========== Page Header ========== #}
    <div class="mb-8">
        <div class="flex items-center gap-2 text-sm text-gray-500 mb-4">
            <a href="{{ path('app_jalon_index') }}" class="hover:text-primary transition-colors">Jalons</a>
            <span class="material-symbols-outlined text-base">chevron_right</span>
            <span class="text-navy dark:text-white">Nouveau</span>
        </div>
        
        <div class="flex items-center gap-4">
            <div class="p-3 bg-primary/20 rounded-xl">
                <span class="material-symbols-outlined text-3xl text-primary">add_circle</span>
            </div>
            <div>
                <h1 class="text-navy dark:text-white text-3xl font-black">Nouveau Jalon</h1>
                <p class="text-gray-500">Créez un nouveau jalon pour marquer une étape importante</p>
            </div>
        </div>
    </div>

    {# ========== Form Card ========== #}
    <div class="max-w-2xl">
        <div class="bg-white dark:bg-gray-900 rounded-xl border border-gray-200 dark:border-gray-800 p-6">
            {{ include('jalon/_form.html.twig', {button_label: 'Créer le jalon'}) }}
        </div>
    </div>
{% endblock %}
