<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Jalons</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; }
        h1 { text-align: center; color: #1e3a5f; margin-bottom: 5px; }
        .date { text-align: center; color: #666; margin-bottom: 20px; font-size: 10px; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background-color: #1e3a5f; color: white; padding: 8px; text-align: left; font-size: 11px; }
        td { padding: 6px 8px; border-bottom: 1px solid #ddd; font-size: 11px; }
        tr:nth-child(even) { background-color: #f9f9f9; }
    </style>
</head>
<body>
    <h1>🎯 Liste des Jalons</h1>
    <p class="date">Généré le {{ "now"|date("d/m/Y H:i") }}</p>
    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Titre</th>
                <th>Échéance</th>
                <th>Statut</th>
                <th>Priorité</th>
                <th>Projet</th>
            </tr>
        </thead>
        <tbody>
            {% for jalon in jalons %}
            <tr>
                <td>{{ jalon.id }}</td>
                <td>{{ jalon.titre }}</td>
                <td>{{ jalon.dateEcheance ? jalon.dateEcheance|date('d/m/Y') : '-' }}</td>
                <td>{{ jalon.statut }}</td>
                <td>{{ jalon.priorite }}</td>
                <td>{{ jalon.projet ? jalon.projet.titre : '-' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    <p style="margin-top: 20px; font-size: 10px; color: #999;">Total: {{ jalons|length }} jalon(s)</p>
</body>
</html>
