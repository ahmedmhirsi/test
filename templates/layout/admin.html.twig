<!DOCTYPE html>
<html class="light" lang="fr">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>{% block title %}SmartNexus AI - Admin{% endblock %}</title>


    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>


    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>


    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#ffb405", // Yellow
                        "accent": "#1A237E", // Navy
                        "navy": "#1A237E",
                        "background-light": "#FFFBF0", // Cream/Light Grey
                        "background-dark": "#231d0f",
                    },
                    fontFamily: { 
                        "display": ["Inter", "sans-serif"],
                        "sans": ["Inter", "sans-serif"]
                    },
                    borderRadius: {
                        "DEFAULT": "0.5rem",
                        "lg": "1rem",
                        "xl": "1.5rem",
                        "full": "9999px"
                    },
                },
            },
        }
    </script>

    <style>
        body { font-family: 'Inter', sans-serif; }
        .material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24; }
        .material-symbols-outlined.filled { font-variation-settings: 'FILL' 1, 'wght' 400, 'GRAD' 0, 'opsz' 24; }
        
        /* Custom scrollbar */
        ::-webkit-scrollbar { width: 8px; height: 8px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: #d1d5db; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #9ca3af; }
        .dark ::-webkit-scrollbar-thumb { background: #4b5563; }
        .dark ::-webkit-scrollbar-thumb:hover { background: #6b7280; }
        
        /* Animation for page transitions */
        .page-enter { animation: fadeIn 0.3s ease-out; }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        /* Admin Sidebar active state - Yellow primary with navy accent */
        .nav-active {
            background: rgba(255, 180, 5, 0.2);
            border: 1px solid rgba(255, 180, 5, 0.3);
        }
        .nav-active span:first-child { color: #ffb405; }
    </style>

    {% block head %}{% endblock %}
</head>

<body class="bg-background-light dark:bg-background-dark text-navy dark:text-white min-h-screen flex flex-col">


<header class="flex items-center justify-between whitespace-nowrap border-b-2 border-solid border-primary/30 dark:border-primary/50 bg-white dark:bg-background-dark px-6 py-3 z-20 sticky top-0">
    <div class="flex items-center gap-8">

        <a href="{{ path('app_dashboard') }}" class="flex items-center gap-3 hover:opacity-90 transition-opacity">
            <img src="{{ asset('assets/logo22.png') }}" alt="SmartNexus Logo" class="h-10 w-auto object-contain">
        </a>





        <form action="{{ path('app_search') }}" method="GET" class="flex flex-col min-w-40 h-10 max-w-72 hidden md:flex">
            <div class="flex w-full flex-1 items-stretch rounded-xl h-full bg-slate-100 dark:bg-slate-800">
                <div class="text-slate-500 flex items-center justify-center pl-4 rounded-l-xl">
                    <span class="material-symbols-outlined">search</span>
                </div>
                <input type="text" name="q" class="form-input flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-xl text-navy dark:text-white focus:outline-0 focus:ring-0 border-none bg-transparent h-full placeholder:text-slate-400 px-4 pl-2 text-sm font-normal"
                       placeholder="Search projects, sprints, tasks..." />
            </div>
        </form>
    </div>

    <div class="flex items-center gap-3">
        {% include 'layout/_header_controls.html.twig' %}
    </div>
</header>

<!-- Top Navigation Bar for Admin Module -->
<nav class="bg-white dark:bg-background-dark border-b border-slate-200 dark:border-slate-700 px-6 py-2">
    <div class="flex items-center justify-between">
        <!-- Main Navigation Links -->
        <div class="flex items-center gap-1">
            <a class="flex items-center gap-2 px-4 py-2 rounded-lg transition-all {{ app.request.attributes.get('_route') == 'app_dashboard' ? 'bg-primary text-navy font-bold shadow-lg' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' }}"
               href="{{ path('app_dashboard') }}">
                <span class="material-symbols-outlined text-lg {{ app.request.attributes.get('_route') == 'app_dashboard' ? 'filled' : '' }}">dashboard</span>
                <span class="text-sm font-medium">Dashboard</span>
            </a>

            <a class="flex items-center gap-2 px-4 py-2 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_projet' ? 'bg-primary text-navy font-bold shadow-lg' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' }}"
               href="{{ path('app_projet_index') }}">
                <span class="material-symbols-outlined text-lg {{ app.request.attributes.get('_route') starts with 'app_projet' ? 'filled' : '' }}">folder_open</span>
                <span class="text-sm font-medium">Projets</span>
            </a>

            <a class="flex items-center gap-2 px-4 py-2 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_sprint' ? 'bg-primary text-navy font-bold shadow-lg' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' }}"
               href="{{ path('app_sprint_index') }}">
                <span class="material-symbols-outlined text-lg {{ app.request.attributes.get('_route') starts with 'app_sprint' ? 'filled' : '' }}">history</span>
                <span class="text-sm font-medium">Sprints</span>
            </a>

            <a class="flex items-center gap-2 px-4 py-2 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_tache' ? 'bg-primary text-navy font-bold shadow-lg' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' }}"
               href="{{ path('app_tache_index') }}">
                <span class="material-symbols-outlined text-lg {{ app.request.attributes.get('_route') starts with 'app_tache' ? 'filled' : '' }}">task_alt</span>
                <span class="text-sm font-medium">Tâches</span>
            </a>

            <a class="flex items-center gap-2 px-4 py-2 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_jalon' ? 'bg-primary text-navy font-bold shadow-lg' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' }}"
               href="{{ path('app_jalon_index') }}">
                <span class="material-symbols-outlined text-lg {{ app.request.attributes.get('_route') starts with 'app_jalon' ? 'filled' : '' }}">flag</span>
                <span class="text-sm font-medium">Jalons</span>
            </a>

            <div class="w-px h-6 bg-slate-200 dark:bg-slate-700 mx-2"></div>

            <a class="flex items-center gap-2 px-4 py-2 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_journal' ? 'bg-primary text-navy font-bold shadow-lg' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' }}"
               href="{{ path('app_journal_temps_index') }}">
                <span class="material-symbols-outlined text-lg {{ app.request.attributes.get('_route') starts with 'app_journal' ? 'filled' : '' }}">schedule</span>
                <span class="text-sm font-medium">Journal (Tous)</span>
            </a>
        </div>

        <!-- Quick Action Button removed for Admin (Read-Only for Sprints) -->
        <div class="flex items-center gap-2 px-4 py-2 bg-slate-100 dark:bg-slate-800 text-slate-500 rounded-lg">
            <span class="material-symbols-outlined text-lg">visibility</span>
            <span class="text-sm font-medium">Vue Supervision</span>
        </div>
    </div>
</nav>

<!-- Main Content Area (Full Width) -->
<main class="flex-1 overflow-y-auto bg-background-light dark:bg-background-dark p-8">
    <div class="max-w-7xl mx-auto page-enter">
        {% block body %}{% endblock %}
    </div>
</main>


{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="fixed bottom-4 right-4 z-50 px-6 py-4 rounded-lg shadow-xl 
                    {{ label == 'success' ? 'bg-green-500' : (label == 'error' ? 'bg-red-500' : 'bg-primary') }} 
                    text-white font-medium animate-pulse">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}

</body>
</html>
