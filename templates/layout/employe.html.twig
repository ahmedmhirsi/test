<!DOCTYPE html>
<html class="light" lang="fr">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>{% block title %}SmartNexus AI - Espace Employé{% endblock %}</title>

    {# Tailwind CDN with plugins #}
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>

    {# Google Fonts #}
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>

    {# Tailwind Configuration - EMPLOYE THEME (Yellow Primary, Navy Accent) #}
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#ffb405",
                        "accent": "#1A237E",
                        "navy": "#1A237E",
                        "background-light": "#FFFBF0",
                        "background-dark": "#231d0f",
                    },
                    fontFamily: { 
                        "display": ["Inter", "sans-serif"],
                        "sans": ["Inter", "sans-serif"]
                    },
                    borderRadius: {
                        "DEFAULT": "0.5rem",
                        "lg": "1rem",
                        "xl": "1.5rem",
                        "full": "9999px"
                    },
                },
            },
        }
    </script>

    <style>
        body { font-family: 'Inter', sans-serif; }
        .material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24; }
        .material-symbols-outlined.filled { font-variation-settings: 'FILL' 1, 'wght' 400, 'GRAD' 0, 'opsz' 24; }
        
        /* Custom scrollbar */
        ::-webkit-scrollbar { width: 8px; height: 8px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: #d1d5db; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #9ca3af; }
        .dark ::-webkit-scrollbar-thumb { background: #4b5563; }
        .dark ::-webkit-scrollbar-thumb:hover { background: #6b7280; }
        
        /* Animation for page transitions */
        .page-enter { animation: fadeIn 0.3s ease-out; }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        /* Employe Sidebar active state - Yellow primary with navy accent */
        .nav-active {
            background: rgba(255, 180, 5, 0.2);
            border: 1px solid rgba(255, 180, 5, 0.3);
        }
        .nav-active span:first-child { color: #ffb405; }
    </style>

    {% block head %}{% endblock %}
</head>

<body class="bg-background-light dark:bg-background-dark text-navy dark:text-white min-h-screen flex flex-col">

{# ========== TOP NAVIGATION BAR - EMPLOYE ========== #}
<header class="flex items-center justify-between whitespace-nowrap border-b-2 border-solid border-primary/30 dark:border-primary/50 bg-white dark:bg-background-dark px-6 py-3 z-20 sticky top-0">
    <div class="flex items-center gap-8">
        {# Logo - Employe Theme (Yellow Primary) #}
        <a href="{{ path('app_kanban_index') }}" class="flex items-center gap-3 text-navy dark:text-primary hover:opacity-90 transition-opacity">
            <div class="size-8 bg-primary rounded-lg flex items-center justify-center text-navy shadow-lg shadow-primary/30">
                <span class="material-symbols-outlined">work</span>
            </div>
            <h2 class="text-navy dark:text-white text-xl font-bold leading-tight tracking-tight">SmartNexus</h2>
        </a>

        {# Employe Badge - Yellow with navy accent #}
        <div class="hidden md:flex items-center gap-2 px-3 py-1 bg-primary/20 dark:bg-primary/30 rounded-full border border-primary/30">
            <span class="material-symbols-outlined text-primary text-sm">person</span>
            <span class="text-navy dark:text-primary text-xs font-bold uppercase tracking-wider">Employé</span>
        </div>

        {# Search Bar #}
        <label class="flex flex-col min-w-40 h-10 max-w-72 hidden md:flex">
            <div class="flex w-full flex-1 items-stretch rounded-xl h-full bg-gray-100 dark:bg-gray-800">
                <div class="text-gray-500 flex items-center justify-center pl-4 rounded-l-xl">
                    <span class="material-symbols-outlined">search</span>
                </div>
                <input class="form-input flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-xl text-navy dark:text-white focus:outline-0 focus:ring-0 border-none bg-transparent h-full placeholder:text-gray-400 px-4 pl-2 text-sm font-normal"
                       placeholder="Rechercher mes tâches..." value=""/>
            </div>
        </label>
    </div>

    <div class="flex items-center gap-3">
        {# Quick Time Log - Yellow Primary #}
        <a href="{{ path('app_journal_temps_new') }}"
           class="hidden sm:flex items-center gap-2 px-4 py-2 bg-primary hover:bg-yellow-500 text-navy font-bold rounded-lg transition-all shadow-lg shadow-primary/20">
            <span class="material-symbols-outlined text-xl">schedule</span>
            <span>Log Time</span>
        </a>

        {# Dark Mode Toggle #}
        <button onclick="document.documentElement.classList.toggle('dark')" 
                class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 text-gray-600 dark:text-gray-400 transition-colors">
            <span class="material-symbols-outlined dark:hidden">dark_mode</span>
            <span class="material-symbols-outlined hidden dark:block">light_mode</span>
        </button>

        {# Notifications #}
        <button class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 text-gray-600 dark:text-gray-400 transition-colors relative">
            <span class="material-symbols-outlined">notifications</span>
            <span class="absolute top-1 right-1 h-2 w-2 bg-red-500 rounded-full"></span>
        </button>

        {# User Avatar - Employe Style (Yellow) #}
        <div class="h-10 w-10 rounded-full border-2 border-primary bg-primary flex items-center justify-center text-navy font-bold cursor-pointer hover:scale-105 transition-transform">
            EM
        </div>
    </div>
</header>

<div class="flex flex-1 overflow-hidden">

    {# ========== SIDEBAR NAVIGATION - EMPLOYE ========== #}
    <aside class="w-64 border-r border-gray-200 dark:border-gray-800 bg-white dark:bg-background-dark flex-col p-4 hidden lg:flex">
        <div class="flex flex-col gap-6 h-full justify-between">
            <div class="flex flex-col gap-1">
                <div class="mb-4 px-2">
                    <h1 class="text-navy dark:text-white text-sm font-bold uppercase tracking-wider">Mon Espace</h1>
                    <p class="text-gray-500 text-xs font-medium">Votre espace de travail</p>
                </div>

                <nav class="flex flex-col gap-1">
                    {# Kanban Board #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_kanban' ? 'nav-active text-navy font-bold' : 'text-gray-600 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800' }}"
                       href="{{ path('app_kanban_index') }}">
                        <span class="material-symbols-outlined {{ app.request.attributes.get('_route') starts with 'app_kanban' ? 'filled' : '' }}">view_kanban</span>
                        <span class="text-sm font-medium">Kanban Board</span>
                    </a>

                    {# Journal de Bord #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_journal' and app.request.attributes.get('_route') != 'app_journal_temps_weekly' ? 'nav-active text-navy font-bold' : 'text-gray-600 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800' }}"
                       href="{{ path('app_journal_temps_index') }}">
                        <span class="material-symbols-outlined {{ app.request.attributes.get('_route') starts with 'app_journal' and app.request.attributes.get('_route') != 'app_journal_temps_weekly' ? 'filled' : '' }}">schedule</span>
                        <span class="text-sm font-medium">Journal de Bord</span>
                    </a>

                    {# Weekly Overview #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') == 'app_journal_temps_weekly' ? 'nav-active text-navy font-bold' : 'text-gray-600 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800' }}"
                       href="{{ path('app_journal_temps_weekly', {year: 'now'|date('Y'), week: 'now'|date('W')}) }}">
                        <span class="material-symbols-outlined">calendar_view_week</span>
                        <span class="text-sm font-medium">Vue Hebdomadaire</span>
                    </a>

                    {# Separator #}
                    <div class="my-3 border-t border-gray-100 dark:border-gray-800"></div>

                    {# Mes Tâches #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_tache' ? 'nav-active text-navy font-bold' : 'text-gray-600 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800' }}"
                       href="{{ path('app_tache_index') }}">
                        <span class="material-symbols-outlined {{ app.request.attributes.get('_route') starts with 'app_tache' ? 'filled' : '' }}">task_alt</span>
                        <span class="text-sm font-medium">Mes Tâches</span>
                    </a>

                    {# Sprints #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_sprint' ? 'nav-active text-navy font-bold' : 'text-gray-600 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800' }}"
                       href="{{ path('app_sprint_index') }}">
                        <span class="material-symbols-outlined {{ app.request.attributes.get('_route') starts with 'app_sprint' ? 'filled' : '' }}">history</span>
                        <span class="text-sm font-medium">Sprints</span>
                    </a>
                </nav>
            </div>

            {# Bottom Actions #}
            <div class="space-y-2">
                <a href="{{ path('app_journal_temps_new') }}" 
                   class="w-full flex items-center justify-center gap-2 rounded-lg h-11 px-4 bg-primary hover:bg-yellow-500 text-navy text-sm font-bold transition-all shadow-lg">
                    <span class="material-symbols-outlined">add</span>
                    <span>Nouvelle Entrée</span>
                </a>
            </div>
        </div>
    </aside>

    {# ========== MAIN CONTENT AREA ========== #}
    <main class="flex-1 overflow-y-auto bg-background-light dark:bg-background-dark p-8">
        <div class="max-w-6xl mx-auto page-enter">
            {% block body %}{% endblock %}
        </div>
    </main>

</div>

{# Flash Messages #}
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="fixed bottom-4 right-4 z-50 px-6 py-4 rounded-lg shadow-xl 
                    {{ label == 'success' ? 'bg-green-500' : (label == 'error' ? 'bg-red-500' : 'bg-primary') }} 
                    text-white font-medium animate-pulse">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}

</body>
</html>
