<!DOCTYPE html>
<html class="light" lang="fr">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>{% block title %}SmartNexus AI{% endblock %}</title>

    {# Tailwind CDN with plugins #}
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>

    {# Google Fonts #}
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>

    {# Tailwind Configuration #}
    <script>
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "nexus-navy": "#10202A",
                        "nexus-dark": "#161516",
                        "nexus-primary": "#B58863",
                        "nexus-accent": "#D3C3B9",
                        "nexus-stone": "#A79E9C",
                        "primary": "#B58863",
                        "navy": "#10202A",
                        "indigo": {
                            50: "#fdfcfb", 100: "#fcf9f7", 200: "#f9f2ed", 300: "#f4e6de", 400: "#e9ccbb",
                            500: "#B58863", 600: "#9E7656", 700: "#846248", 800: "#694e3a", 900: "#564030", 950: "#2d2119",
                        },
                         "gray": {
                            950: "#0b151b", 900: "#10202A", 800: "#161516", 700: "#3D4D55", 600: "#475569",
                            500: "#A79E9C", 400: "#D3C3B9", 300: "#cbd5e1", 200: "#e2e8f0", 100: "#f1f5f9", 50: "#f8fafc"
                        },
                        "slate": {
                            950: "#0b151b", 900: "#10202A", 800: "#161516", 700: "#3D4D55", 600: "#475569",
                            500: "#A79E9C", 400: "#D3C3B9", 300: "#cbd5e1", 200: "#e2e8f0", 100: "#f1f5f9", 50: "#f8fafc"
                        }
                    },
                    fontFamily: {
                        display: ["Inter", "Manrope", "sans-serif"],
                        body: ["Inter", "sans-serif"]
                    }
                },
            },
        }
    </script>

    <style>
        body { font-family: 'Inter', sans-serif; }
        .material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24; }
        .material-symbols-outlined.filled { font-variation-settings: 'FILL' 1, 'wght' 400, 'GRAD' 0, 'opsz' 24; }
        
        /* Custom scrollbar */
        ::-webkit-scrollbar { width: 8px; height: 8px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: #d1d5db; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #9ca3af; }
        .dark ::-webkit-scrollbar-thumb { background: #4b5563; }
        .dark ::-webkit-scrollbar-thumb:hover { background: #6b7280; }
        
        /* Animation for page transitions */
        .page-enter { animation: fadeIn 0.3s ease-out; }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        /* Sidebar active state */
        .nav-active {
            background: rgba(181, 136, 99, 0.2);
            border: 1px solid rgba(181, 136, 99, 0.3);
        }
    </style>

    {% block head %}{% endblock %}
</head>

<body class="bg-[#10202A] text-[#D3C3B9]/80 antialiased min-h-screen flex flex-col font-body selection:bg-nexus-primary selection:text-black">

{# ========== TOP NAVIGATION BAR ========== #}
<header class="flex items-center justify-between whitespace-nowrap border-b border-white/5 bg-[#161516]/80 backdrop-blur-xl px-6 py-3 z-20 sticky top-0 shadow-xl shadow-black/20">
    <div class="flex items-center gap-8">
        {# Logo #}
        <a href="{{ path('app_dashboard') }}" class="flex items-center gap-3 hover:opacity-90 transition-opacity">
            <div class="size-9 bg-nexus-primary rounded-xl flex items-center justify-center text-white shadow-lg shadow-nexus-primary/20">
                <span class="material-symbols-outlined text-[22px]">dataset</span>
            </div>
            <h2 class="text-white text-xl font-black tracking-tight">SmartNexus Suite</h2>
        </a>

        {# Search Bar #}
        <form action="{{ path('app_search') }}" method="GET" class="flex flex-col min-w-40 h-10 max-w-72 hidden md:flex">
            <div class="flex w-full flex-1 items-stretch rounded-xl h-full bg-white/5 border border-white/10">
                <div class="text-[#A79E9C] flex items-center justify-center pl-4 rounded-l-xl">
                    <span class="material-symbols-outlined text-[20px]">search</span>
                </div>
                <input type="text" name="q" class="form-input flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-xl text-white focus:outline-0 focus:ring-0 border-none bg-transparent h-full placeholder:text-[#A79E9C]/50 px-4 pl-2 text-sm font-normal"
                       placeholder="Search infrastructure..." />
            </div>
        </form>
    </div>

    <div class="flex items-center gap-3">
        {# Quick Actions #}
        <a href="{{ path('app_sprint_new') }}"
           class="hidden sm:flex items-center gap-2 px-6 py-2 bg-nexus-primary hover:bg-nexus-primary/90 text-black font-black uppercase tracking-widest text-[11px] rounded-xl transition-all shadow-xl shadow-nexus-primary/20">
            <span class="material-symbols-outlined text-[18px]">add</span>
            <span>New Sprint</span>
        </a>

        {# Dark Mode Toggle #}
        <button onclick="document.documentElement.classList.toggle('dark')" 
                class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 text-gray-600 dark:text-gray-400 transition-colors">
            <span class="material-symbols-outlined dark:hidden">dark_mode</span>
            <span class="material-symbols-outlined hidden dark:block">light_mode</span>
        </button>

        {# Notifications #}
        <button class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800 text-gray-600 dark:text-gray-400 transition-colors relative">
            <span class="material-symbols-outlined">notifications</span>
            <span class="absolute top-1 right-1 h-2 w-2 bg-red-500 rounded-full"></span>
        </button>

        {# User Avatar #}
        <div class="h-10 w-10 rounded-xl bg-[#3D4D55] flex items-center justify-center text-[#D3C3B9] font-black border border-white/10 cursor-pointer hover:border-nexus-primary/50 transition-all">
            PM
        </div>
    </div>
</header>

<div class="flex flex-1 overflow-hidden">

    {# ========== SIDEBAR NAVIGATION ========== #}
    <aside class="w-68 border-r border-white/5 bg-[#161516]/80 backdrop-blur-xl flex-col p-6 hidden lg:flex">
        <div class="flex flex-col gap-6 h-full justify-between">
            <div class="flex flex-col gap-1">
                <div class="mb-4 px-2">
                    <h1 class="text-navy dark:text-white text-sm font-bold uppercase tracking-wider">Project Manager</h1>
                    <p class="text-gray-500 text-xs font-medium">Sprint Management Suite</p>
                </div>

                <nav class="flex flex-col gap-1">
                    {# Dashboard #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') == 'app_dashboard' ? 'bg-nexus-primary text-black font-black shadow-xl shadow-nexus-primary/20' : 'text-[#A79E9C] hover:bg-white/5 hover:text-white' }}"
                       href="{{ path('app_dashboard') }}">
                        <span class="material-symbols-outlined {{ app.request.attributes.get('_route') == 'app_dashboard' ? 'filled' : '' }}">dashboard</span>
                        <span class="text-sm font-medium">Dashboard</span>
                    </a>

                    {# Projets #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_projet' ? 'bg-nexus-primary text-black font-black shadow-xl shadow-nexus-primary/20' : 'text-[#A79E9C] hover:bg-white/5 hover:text-white' }}"
                       href="{{ path('app_projet_index') }}">
                        <span class="material-symbols-outlined {{ app.request.attributes.get('_route') starts with 'app_projet' ? 'filled' : '' }}">folder_open</span>
                        <span class="text-sm font-medium">Projets</span>
                    </a>

                    {# Sprints #}
                    <a class="flex items-center gap-3 px-4 py-3 rounded-xl transition-all {{ app.request.attributes.get('_route') starts with 'app_sprint' ? 'bg-nexus-primary text-black font-black shadow-xl shadow-nexus-primary/20' : 'text-[#A79E9C] hover:bg-white/5 hover:text-white' }}"
                       href="{{ path('app_sprint_index') }}">
                        <span class="material-symbols-outlined text-[20px]">history</span>
                        <span class="text-sm">Sprints</span>
                    </a>

                    {# Tâches #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_tache' ? 'bg-nexus-primary text-black font-black shadow-xl shadow-nexus-primary/20' : 'text-[#A79E9C] hover:bg-white/5 hover:text-white' }}"
                       href="{{ path('app_tache_index') }}">
                        <span class="material-symbols-outlined {{ app.request.attributes.get('_route') starts with 'app_tache' ? 'filled' : '' }}">task_alt</span>
                        <span class="text-sm font-medium">Tâches</span>
                    </a>

                    {# Jalons #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_jalon' ? 'bg-nexus-primary text-black font-black shadow-xl shadow-nexus-primary/20' : 'text-[#A79E9C] hover:bg-white/5 hover:text-white' }}"
                       href="{{ path('app_jalon_index') }}">
                        <span class="material-symbols-outlined {{ app.request.attributes.get('_route') starts with 'app_jalon' ? 'filled' : '' }}">flag</span>
                        <span class="text-sm font-medium">Jalons</span>
                    </a>

                    {# Kanban Board #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_kanban' ? 'bg-nexus-primary text-black font-black shadow-xl shadow-nexus-primary/20' : 'text-[#A79E9C] hover:bg-white/5 hover:text-white' }}"
                       href="{{ path('app_kanban_index') }}">
                        <span class="material-symbols-outlined {{ app.request.attributes.get('_route') starts with 'app_kanban' ? 'filled' : '' }}">view_kanban</span>
                        <span class="text-sm font-medium">Kanban Board</span>
                    </a>

                    {# Journal de Bord #}
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-lg transition-all {{ app.request.attributes.get('_route') starts with 'app_journal' ? 'bg-nexus-primary text-black font-black shadow-xl shadow-nexus-primary/20' : 'text-[#A79E9C] hover:bg-white/5 hover:text-white' }}"
                       href="{{ path('app_journal_temps_index') }}">
                        <span class="material-symbols-outlined {{ app.request.attributes.get('_route') starts with 'app_journal' ? 'filled' : '' }}">schedule</span>
                        <span class="text-sm font-medium">Journal de Bord</span>
                    </a>
                </nav>

                {# Separator #}
                <div class="my-4 border-t border-gray-100 dark:border-gray-800"></div>

                {# Sprint Quick Access #}
                <div class="px-2 mb-2">
                    <p class="text-gray-400 text-xs font-bold uppercase tracking-wider">Sprints Récents</p>
                </div>
                <nav class="flex flex-col gap-1" id="recent-sprints">
                    {# These would be populated dynamically, showing as placeholders #}
                    <a class="flex items-center gap-3 px-3 py-2 rounded-lg text-gray-500 hover:bg-gray-100 dark:hover:bg-gray-800 transition-all text-sm"
                       href="{{ path('app_sprint_index') }}">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        <span class="truncate">Voir tous les sprints</span>
                    </a>
                </nav>
            </div>

            {# Bottom Actions #}
            <div class="pt-6 border-t border-white/5">
                <a href="{{ path('app_sprint_new') }}" 
                   class="w-full flex items-center justify-center gap-2 rounded-xl h-12 px-4 bg-nexus-primary hover:bg-nexus-primary/90 text-black text-sm font-black uppercase tracking-widest transition-all shadow-xl shadow-nexus-primary/20">
                    <span class="material-symbols-outlined text-[20px]">add</span>
                    <span>New Sprint</span>
                </a>
            </div>
        </div>
    </aside>

    {# ========== MAIN CONTENT AREA ========== #}
    <main class="flex-1 overflow-y-auto bg-[#161516]/20 p-8 no-scrollbar">
        <div class="max-w-7xl mx-auto page-enter">
            {% block body %}{% endblock %}
        </div>
    </main>

</div>

{# Flash Messages #}
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="fixed bottom-4 right-4 z-50 px-6 py-4 rounded-lg shadow-xl 
                    {{ label == 'success' ? 'bg-nexus-primary' : (label == 'error' ? 'bg-rose-500' : 'bg-nexus-navy') }} 
                    text-black font-black uppercase tracking-widest text-[11px] shadow-2xl border border-white/10">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}

</body>
</html>
