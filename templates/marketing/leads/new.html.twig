{% extends 'marketing/base.html.twig' %}

{% block title %}New Lead{% endblock %}

{% block body %}
<div class="max-w-2xl mx-auto fade-in">
    <div class="mb-6">
        <a href="{{ path('app_marketing_leads') }}" class="back-link">
            <span class="material-symbols-outlined text-[18px]">arrow_back</span>
            Back to Leads
        </a>
    </div>

    <div class="section-card p-6 rise-in">
        <div class="mb-6">
            <h1 class="text-2xl font-display font-black text-navy dark:text-white tracking-tight">Add New Lead</h1>
            <p class="text-gray-500 dark:text-gray-400 text-sm mt-1">Capture a new B2B prospect</p>
        </div>

        {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                    {{ form_label(form.contactName, null, {'label_attr': {'class': 'block text-sm font-bold text-navy dark:text-white mb-2'}}) }}
                    {{ form_widget(form.contactName, {'attr': {'class': 'form-input-lg'}}) }}
                </div>

                <div>
                    {{ form_label(form.companyName, null, {'label_attr': {'class': 'block text-sm font-bold text-navy dark:text-white mb-2'}}) }}
                    {{ form_widget(form.companyName, {'attr': {'class': 'form-input-lg'}}) }}
                </div>

                <div>
                    {{ form_label(form.email, null, {'label_attr': {'class': 'block text-sm font-bold text-navy dark:text-white mb-2'}}) }}
                    {{ form_widget(form.email, {'attr': {'class': 'form-input-lg'}}) }}
                </div>

                <div>
                    {{ form_label(form.phone, null, {'label_attr': {'class': 'block text-sm font-bold text-navy dark:text-white mb-2'}}) }}
                    {{ form_widget(form.phone, {'attr': {'class': 'form-input-lg'}}) }}
                </div>

                <div>
                    {{ form_label(form.position, null, {'label_attr': {'class': 'block text-sm font-bold text-navy dark:text-white mb-2'}}) }}
                    {{ form_widget(form.position, {'attr': {'class': 'form-input-lg'}}) }}
                </div>

                <div>
                    {{ form_label(form.status, null, {'label_attr': {'class': 'block text-sm font-bold text-navy dark:text-white mb-2'}}) }}
                    {{ form_widget(form.status, {'attr': {'class': 'form-input-lg'}}) }}
                </div>

                <div>
                    {{ form_label(form.campaign, null, {'label_attr': {'class': 'block text-sm font-bold text-navy dark:text-white mb-2'}}) }}
                    {{ form_widget(form.campaign, {'attr': {'class': 'form-input-lg'}}) }}
                </div>

                <div>
                    {{ form_label(form.channel, null, {'label_attr': {'class': 'block text-sm font-bold text-navy dark:text-white mb-2'}}) }}
                    {{ form_widget(form.channel, {'attr': {'class': 'form-input-lg'}}) }}
                </div>
            </div>

            <div class="flex gap-4 pt-4">
                <button type="submit" class="btn-primary flex-1 h-12 text-sm">
                    Add Lead
                </button>
                <a href="{{ path('app_marketing_leads') }}" class="btn-secondary h-12 text-sm">
                    Cancel
                </a>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
