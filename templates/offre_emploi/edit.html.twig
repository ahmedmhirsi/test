{% extends 'base.html.twig' %}

{% block title %}Modifier Offre{% endblock %}

{% block body %}
    <div class="max-w-3xl mx-auto">
        <div class="flex justify-between items-center mb-8">
            <div>
                <h1 class="text-2xl font-bold text-gray-900">Modifier l'Offre</h1>
                <p class="text-gray-500 mt-1">Update job details and requirements.</p>
            </div>
             <div class="flex items-center gap-2">
                 {{ include('offre_emploi/_delete_form.html.twig') }}
            </div>
        </div>

        <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 md:p-8">
            {{ include('offre_emploi/_form.html.twig', {'button_label': 'Update Job'}) }}
        </div>
    </div>
{% endblock %}
