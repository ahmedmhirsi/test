<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Offres d'Emploi</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; }
        h1 { text-align: center; color: #1e3a5f; margin-bottom: 5px; }
        .date { text-align: center; color: #666; margin-bottom: 20px; font-size: 10px; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background-color: #1e3a5f; color: white; padding: 8px; text-align: left; font-size: 11px; }
        td { padding: 6px 8px; border-bottom: 1px solid #ddd; font-size: 11px; }
        tr:nth-child(even) { background-color: #f9f9f9; }
    </style>
</head>
<body>
    <h1>💼 Liste des Offres d'Emploi</h1>
    <p class="date">Généré le {{ "now"|date("d/m/Y H:i") }}</p>
    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Poste</th>
                <th>Type Contrat</th>
                <th>Salaire Min</th>
                <th>Salaire Max</th>
                <th>Publication</th>
                <th>Statut</th>
            </tr>
        </thead>
        <tbody>
            {% for offre in offres %}
            <tr>
                <td>{{ offre.id }}</td>
                <td>{{ offre.poste }}</td>
                <td>{{ offre.typeContrat }}</td>
                <td>{{ offre.salaireMin ? offre.salaireMin|number_format(2, ',', ' ') ~ ' DT' : '-' }}</td>
                <td>{{ offre.salaireMax ? offre.salaireMax|number_format(2, ',', ' ') ~ ' DT' : '-' }}</td>
                <td>{{ offre.datePublication|date('d/m/Y') }}</td>
                <td>{{ offre.statut }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    <p style="margin-top: 20px; font-size: 10px; color: #999;">Total: {{ offres|length }} offre(s)</p>
</body>
</html>
