{% extends 'front_office/base.html.twig' %}

{% block title %}Codes de secours 2FA - SmartNexus AI{% endblock %}

{% block content %}
<div class="min-h-screen bg-gray-50">
    <!-- Header -->
    <div class="bg-gradient-to-r from-navy to-electric px-6 py-8">
        <div class="max-w-4xl mx-auto">
            <div class="flex items-center gap-4">
                <a href="{{ path('app_2fa_settings') }}" class="w-10 h-10 rounded-lg bg-white/10 flex items-center justify-center text-white hover:bg-white/20 transition-colors">
                    <span class="material-symbols-outlined">arrow_back</span>
                </a>
                <div class="text-white">
                    <h1 class="text-2xl font-bold">Codes de secours</h1>
                    <p class="text-blue-100">{% if isFirstTime %}Étape 3 sur 3 : Sauvegardez vos codes{% else %}Gérez vos codes de récupération{% endif %}</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Content -->
    <div class="max-w-2xl mx-auto px-6 py-8">
        <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-8">
            {% if isFirstTime %}
                <!-- Success Message -->
                <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
                    <div class="flex items-start gap-3">
                        <span class="material-symbols-outlined text-green-600 text-2xl">check_circle</span>
                        <div>
                            <p class="font-semibold text-green-900 mb-1">Authentification 2FA activée avec succès !</p>
                            <p class="text-sm text-green-700">Votre compte est maintenant protégé par une double authentification.</p>
                        </div>
                    </div>
                </div>
            {% endif %}

            <!-- Warning -->
            <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-6">
                <div class="flex items-start gap-3">
                    <span class="material-symbols-outlined text-amber-600 text-xl">warning</span>
                    <div class="flex-1">
                        <p class="font-semibold text-amber-900 mb-1">⚠️ Important : Conservez ces codes en lieu sûr</p>
                        <p class="text-sm text-amber-700">
                            Chaque code ne peut être utilisé qu'une seule fois. Ces codes vous permettront d'accéder à votre compte si vous perdez accès à votre application d'authentification.
                        </p>
                    </div>
                </div>
            </div>

            <!-- Instructions -->
            <div class="mb-6">
                <h3 class="font-bold text-gray-900 mb-2">Comment utiliser ces codes ?</h3>
                <ul class="text-sm text-gray-600 space-y-2">
                    <li class="flex items-start gap-2">
                        <span class="material-symbols-outlined text-primary text-lg mt-0.5">check_circle</span>
                        <span>Chaque code peut être utilisé <strong>une seule fois</strong></span>
                    </li>
                    <li class="flex items-start gap-2">
                        <span class="material-symbols-outlined text-primary text-lg mt-0.5">check_circle</span>
                        <span>Utilisez-les si vous n'avez pas accès à votre application d'authentification</span>
                    </li>
                    <li class="flex items-start gap-2">
                        <span class="material-symbols-outlined text-primary text-lg mt-0.5">check_circle</span>
                        <span>Imprimez-les ou sauvegardez-les dans un endroit sûr</span>
                    </li>
                </ul>
            </div>

            <!-- Backup Codes -->
            <div class="mb-6">
                <div class="bg-gray-50 border-2 border-dashed border-gray-300 rounded-lg p-6">
                    <div class="flex items-center justify-between mb-4">
                        <h4 class="font-bold text-gray-900">Vos codes de secours</h4>
                        <button onclick="copyAllCodes()" class="text-sm text-navy font-semibold hover:underline flex items-center gap-1">
                            <span class="material-symbols-outlined text-lg">content_copy</span>
                            Copier tout
                        </button>
                    </div>
                    <div class="grid grid-cols-2 gap-3" id="codes-container">
                        {% for code in backupCodes %}
                            <div class="font-mono text-center py-3 px-4 bg-white border border-gray-200 rounded-lg text-navy font-bold text-lg tracking-wider">
                                {{ code }}
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>

            <!-- Actions -->
            <div class="flex flex-col sm:flex-row gap-4">
                <button onclick="printCodes()" class="flex-1 inline-flex items-center justify-center gap-2 px-6 py-3 border border-gray-200 text-gray-700 font-semibold rounded-lg hover:bg-gray-50 transition-colors">
                    <span class="material-symbols-outlined">print</span>
                    Imprimer les codes
                </button>
                <button onclick="downloadCodes()" class="flex-1 inline-flex items-center justify-center gap-2 px-6 py-3 border border-gray-200 text-gray-700 font-semibold rounded-lg hover:bg-gray-50 transition-colors">
                    <span class="material-symbols-outlined">download</span>
                    Télécharger (TXT)
                </button>
            </div>

            {% if isFirstTime %}
                <div class="mt-6 pt-6 border-t border-gray-200">
                    <a href="{{ path('app_dashboard') }}" class="w-full inline-flex items-center justify-center gap-2 px-6 py-3 bg-primary text-navy rounded-lg font-bold hover:bg-yellow-400 transition-colors shadow-lg shadow-primary/30">
                        <span class="material-symbols-outlined">check</span>
                        J'ai sauvegardé mes codes, continuer
                    </a>
                </div>
            {% endif %}
        </div>
    </div>
</div>

<script>
const backupCodes = {{ backupCodes|json_encode|raw }};

function copyAllCodes() {
    const codesText = backupCodes.join('\n');
    navigator.clipboard.writeText(codesText).then(function() {
        showNotification('Codes copiés dans le presse-papiers !');
    });
}

function printCodes() {
    const printWindow = window.open('', '', 'height=600,width=800');
    printWindow.document.write(`
        <html>
        <head>
            <title>Codes de secours 2FA - SmartNexus</title>
            <style>
                body { 
                    font-family: Arial, sans-serif; 
                    padding: 40px;
                    max-width: 800px;
                    margin: 0 auto;
                }
                h1 { 
                    color: #1e293b; 
                    margin-bottom: 20px;
                }
                .warning {
                    background: #fef3c7;
                    border: 2px solid #f59e0b;
                    padding: 15px;
                    border-radius: 8px;
                    margin-bottom: 30px;
                }
                .codes {
                    display: grid;
                    grid-template-columns: repeat(2, 1fr);
                    gap: 15px;
                    margin: 30px 0;
                }
                .code {
                    font-family: 'Courier New', monospace;
                    font-size: 18px;
                    font-weight: bold;
                    padding: 15px;
                    border: 2px solid #e5e7eb;
                    border-radius: 8px;
                    text-align: center;
                    background: #f9fafb;
                }
                .instructions {
                    margin-top: 30px;
                    padding-top: 20px;
                    border-top: 2px solid #e5e7eb;
                }
                @media print {
                    body { padding: 20px; }
                }
            </style>
        </head>
        <body>
            <h1>🔐 Codes de secours - SmartNexus AI</h1>
            <div class="warning">
                <strong>⚠️ Important :</strong> Conservez ces codes en lieu sûr. Chaque code ne peut être utilisé qu'une seule fois.
            </div>
            <div class="codes">
                ${backupCodes.map(code => `<div class="code">${code}</div>`).join('')}
            </div>
            <div class="instructions">
                <h3>Instructions :</h3>
                <ul>
                    <li>Utilisez ces codes si vous n'avez pas accès à votre application d'authentification</li>
                    <li>Chaque code est à usage unique</li>
                    <li>Conservez ce document dans un endroit sûr</li>
                    <li>Vous pouvez générer de nouveaux codes depuis votre profil</li>
                </ul>
                <p style="margin-top: 20px; color: #6b7280; font-size: 12px;">
                    Imprimé le ${new Date().toLocaleDateString('fr-FR')} à ${new Date().toLocaleTimeString('fr-FR')}
                </p>
            </div>
        </body>
        </html>
    `);
    printWindow.document.close();
    printWindow.print();
}

function downloadCodes() {
    const content = `CODES DE SECOURS 2FA - SmartNexus AI
=====================================

⚠️ IMPORTANT : Conservez ces codes en lieu sûr
Chaque code ne peut être utilisé qu'une seule fois.

${backupCodes.map((code, index) => `${index + 1}. ${code}`).join('\n')}

INSTRUCTIONS :
- Utilisez ces codes si vous n'avez pas accès à votre application d'authentification
- Chaque code est à usage unique  
- Conservez ce fichier dans un endroit sûr
- Vous pouvez générer de nouveaux codes depuis votre profil

Généré le ${new Date().toLocaleDateString('fr-FR')} à ${new Date().toLocaleTimeString('fr-FR')}
`;

    const blob = new Blob([content], { type: 'text/plain' });
    const url = window.URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `smartnexus-backup-codes-${Date.now()}.txt`;
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    window.URL.revokeObjectURL(url);
    
    showNotification('Codes téléchargés !');
}

function showNotification(message) {
    const notification = document.createElement('div');
    notification.className = 'fixed top-4 right-4 bg-green-500 text-white px-6 py-3 rounded-lg shadow-lg z-50 flex items-center gap-2';
    notification.innerHTML = `
        <span class="material-symbols-outlined">check_circle</span>
        <span>${message}</span>
    `;
    document.body.appendChild(notification);
    
    setTimeout(() => {
        notification.remove();
    }, 3000);
}
</script>
{% endblock %}
