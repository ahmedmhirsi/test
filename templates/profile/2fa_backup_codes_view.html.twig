{% extends 'front_office/base.html.twig' %}

{% block title %}Mes codes de secours - SmartNexus AI{% endblock %}

{% block content %}
<div class="min-h-screen bg-gray-50">
    <!-- Header -->
    <div class="bg-gradient-to-r from-navy to-electric px-6 py-8">
        <div class="max-w-4xl mx-auto">
            <div class="flex items-center gap-4">
                <a href="{{ path('app_2fa_settings') }}" class="w-10 h-10 rounded-lg bg-white/10 flex items-center justify-center text-white hover:bg-white/20 transition-colors">
                    <span class="material-symbols-outlined">arrow_back</span>
                </a>
                <div class="text-white">
                    <h1 class="text-2xl font-bold">Codes de secours</h1>
                    <p class="text-blue-100">Gérez vos codes de récupération</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Content -->
    <div class="max-w-2xl mx-auto px-6 py-8">
        <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-8">
            <!-- Warning -->
            <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-6">
                <div class="flex items-start gap-3">
                    <span class="material-symbols-outlined text-amber-600 text-xl">info</span>
                    <div class="flex-1">
                        <p class="text-sm text-amber-900">
                            Les codes utilisés ne peuvent plus être réutilisés. Si vous avez utilisé tous vos codes ou les avez perdus, vous pouvez en générer de nouveaux.
                        </p>
                    </div>
                </div>
            </div>

            <!-- Backup Codes Status -->
            <div class="mb-6">
                <h3 class="font-bold text-gray-900 mb-4">État de vos codes</h3>
                <div class="grid grid-cols-2 gap-3">
                    {% for code in backupCodes %}
                        <div class="relative font-mono text-center py-3 px-4 {% if code.used %}bg-gray-100 text-gray-400{% else %}bg-white border border-gray-200 text-navy font-bold{% endif %} rounded-lg text-lg tracking-wider">
                            {{ loop.index }}. ****-****
                            {% if code.used %}
                                <div class="absolute inset-0 flex items-center justify-center">
                                    <span class="bg-red-500 text-white text-xs px-2 py-0.5 rounded font-bold">UTILISÉ</span>
                                </div>
                                <p class="text-xs text-gray-500 mt-1">{{ code.usedAt|date('d/m/Y H:i') }}</p>
                            {% else %}
                                <span class="text-xs text-green-600 font-normal">Disponible</span>
                            {% endif %}
                        </div>
                    {% endfor %}
                </div>
                
                <div class="mt-4 flex items-center justify-between p-4 bg-gray-50 rounded-lg">
                    <div>
                        <p class="text-sm font-medium text-gray-900">
                            {{ backupCodes|filter(code => not code.used)|length }} / {{ backupCodes|length }} codes disponibles
                        </p>
                        <p class="text-xs text-gray-600">
                            {% set availableCount = backupCodes|filter(code => not code.used)|length %}
                            {% if availableCount == 0 %}
                                ⚠️ Aucun code disponible - Générez-en de nouveaux
                            {% elseif availableCount <= 3 %}
                                ⚠️ Peu de codes restants - Pensez à en générer de nouveaux
                            {% else %}
                                ✅ Vous avez assez de codes de secours
                            {% endif %}
                        </p>
                    </div>
                </div>
            </div>

            <!-- Regenerate Form -->
            <div class="pt-6 border-t border-gray-200">
                <h4 class="font-bold text-gray-900 mb-4">Générer de nouveaux codes</h4>
                <p class="text-sm text-gray-600 mb-4">
                    La génération de nouveaux codes va invalider tous les codes actuels (utilisés et non utilisés). Vous recevrez 10 nouveaux codes.
                </p>
                
                <button type="button" onclick="showRegenerateModal()" class="w-full inline-flex items-center justify-center gap-2 px-6 py-3 bg-navy text-white rounded-lg font-semibold hover:bg-navy/90 transition-colors">
                    <span class="material-symbols-outlined">refresh</span>
                    Générer de nouveaux codes
                </button>
            </div>

            <!-- Back Link -->
            <div class="mt-6 text-center">
                <a href="{{ path('app_2fa_settings') }}" class="text-sm text-gray-600 hover:text-navy font-medium">
                    ← Retour aux paramètres 2FA
                </a>
            </div>
        </div>
    </div>
</div>

<!-- Modal de régénération -->
<div id="regenerateModal" class="hidden fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4">
    <div class="bg-white rounded-2xl shadow-xl max-w-md w-full p-6">
        <div class="flex items-center gap-3 mb-4">
            <div class="w-12 h-12 rounded-full bg-amber-100 flex items-center justify-center">
                <span class="material-symbols-outlined text-amber-600 text-2xl">warning</span>
            </div>
            <h3 class="text-xl font-bold text-gray-900">Générer nouveaux codes ?</h3>
        </div>
        
        <p class="text-gray-600 mb-6">
            Cette action va invalider tous vos codes actuels. Pour confirmer, entrez votre mot de passe.
        </p>
        
        <form action="{{ path('app_2fa_backup_codes_regenerate') }}" method="post">
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('regenerate_backup_codes') }}">
            
            <div class="mb-6">
                <label class="block text-sm font-medium text-gray-700 mb-2">Mot de passe actuel</label>
                <input type="password" name="password" required class="w-full rounded-lg border border-gray-200 px-4 py-3 focus:border-primary focus:ring-1 focus:ring-primary outline-none transition-all" placeholder="Entrez votre mot de passe">
            </div>
            
            <div class="flex gap-3">
                <button type="button" onclick="hideRegenerateModal()" class="flex-1 px-4 py-3 border border-gray-200 text-gray-700 font-semibold rounded-lg hover:bg-gray-50 transition-colors">
                    Annuler
                </button>
                <button type="submit" class="flex-1 px-4 py-3 bg-navy text-white font-semibold rounded-lg hover:bg-navy/90 transition-colors">
                    Générer
                </button>
            </div>
        </form>
    </div>
</div>

<script>
function showRegenerateModal() {
    document.getElementById('regenerateModal').classList.remove('hidden');
}

function hideRegenerateModal() {
    document.getElementById('regenerateModal').classList.add('hidden');
}

// Close modal on escape key
document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
        hideRegenerateModal();
    }
});

// Close modal on backdrop click
document.getElementById('regenerateModal')?.addEventListener('click', function(e) {
    if (e.target === this) {
        hideRegenerateModal();
    }
});
</script>
{% endblock %}
