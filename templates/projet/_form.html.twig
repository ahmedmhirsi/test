{{ form_start(form, {'attr': {'class': 'space-y-6', 'novalidate': 'novalidate'}}) }}
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="md:col-span-2">
            {{ form_row(form.titre, {'attr': {'placeholder': 'Ex: Refonte Platforme Nexus V2'}}) }}
        </div>

        <div class="md:col-span-2">
            {{ form_row(form.description, {'attr': {'placeholder': 'Synthèse des objectifs stratégiques...'}}) }}
        </div>

        {{ form_row(form.dateDebut) }}
        {{ form_row(form.dateFin) }}

        {{ form_row(form.budget, {'attr': {'placeholder': 'Ex: 15000'}}) }}
        
        <div class="grid grid-cols-2 gap-4">
             {{ form_row(form.statut) }}
             {{ form_row(form.priorite) }}
        </div>
    </div>

    <div class="pt-8 border-t border-white/5 flex justify-end">
        <button class="btn-nexus-primary rounded-xl px-10 py-4 text-sm shadow-xl shadow-nexus-primary/20 group">
             <span class="material-symbols-outlined text-[20px] group-hover:rotate-12 transition-transform">rocket_launch</span>
             <span class="ml-2">{{ button_label|default('Enregistrer') }}</span>
        </button>
    </div>

{{ form_end(form) }}
