{% extends 'back_office/base.html.twig' %}

{% block title %}Update Project – SmartNexus AI{% endblock %}

{% block header_title %}
    <span class="flex items-center gap-2">
        <span class="material-symbols-outlined text-nexus-primary">settings_applications</span>
        Infrastructure Update
    </span>
{% endblock %}

{% block content %}
<div class="swup-transition-fade pb-20">
    <div class="max-w-3xl mx-auto">
        
        {# ─── Header ─── #}
        <div class="mb-8 flex items-end justify-between px-4 animate-card">
            <div>
                 <nav class="flex items-center gap-2 text-[11px] font-bold uppercase tracking-wider text-slate-500 mb-2">
                    <a href="{{ path('app_projet_index') }}" class="hover:text-nexus-primary transition-colors">Portfolio</a>
                    <span class="material-symbols-outlined text-[16px]">chevron_right</span>
                    <a href="{{ path('app_projet_show', {id: projet.id}) }}" class="hover:text-nexus-primary transition-colors">{{ projet.titre }}</a>
                </nav>
                <h1 class="text-3xl font-extrabold text-white tracking-tight">Configure <span class="text-slate-400">Project</span></h1>
            </div>
            
            <a href="{{ path('app_projet_show', {id: projet.id}) }}" class="flex items-center gap-2 text-[11px] font-bold uppercase tracking-wider text-slate-500 hover:text-white transition-colors">
                <span class="material-symbols-outlined text-[20px]">arrow_back</span>
                Back to Dashboard
            </a>
        </div>

        {# ─── Form Container ─── #}
        <div class="glass-panel p-10 animate-card bg-slate-900/40 relative overflow-hidden group" style="animation-delay: 0.1s">
            <div class="relative z-10">
                {{ include('projet/_form.html.twig', {'button_label': 'Update Configuration'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
