<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des Tâches</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; }
        h1 { text-align: center; color: #1e3a5f; margin-bottom: 5px; }
        .date { text-align: center; color: #666; margin-bottom: 20px; font-size: 10px; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background-color: #1e3a5f; color: white; padding: 8px; text-align: left; font-size: 11px; }
        td { padding: 6px 8px; border-bottom: 1px solid #ddd; font-size: 11px; }
        tr:nth-child(even) { background-color: #f9f9f9; }
        .badge { padding: 2px 6px; border-radius: 3px; font-size: 10px; color: white; }
        .badge-todo { background-color: #6c757d; }
        .badge-in_progress { background-color: #007bff; }
        .badge-review { background-color: #ffc107; color: #333; }
        .badge-done { background-color: #28a745; }
    </style>
</head>
<body>
    <h1>✅ Liste des Tâches</h1>
    <p class="date">Généré le {{ "now"|date("d/m/Y H:i") }}</p>
    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Titre</th>
                <th>Statut</th>
                <th>Priorité</th>
                <th>Temps Estimé (h)</th>
                <th>Temps Réel (h)</th>
            </tr>
        </thead>
        <tbody>
            {% for tache in taches %}
            <tr>
                <td>{{ tache.id }}</td>
                <td>{{ tache.titre }}</td>
                <td><span class="badge badge-{{ tache.statut }}">{{ tache.statut }}</span></td>
                <td>{{ tache.priorite }}</td>
                <td>{{ tache.tempsEstime ?? '-' }}</td>
                <td>{{ tache.tempsReel ?? '-' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    <p style="margin-top: 20px; font-size: 10px; color: #999;">Total: {{ taches|length }} tâche(s)</p>
</body>
</html>
