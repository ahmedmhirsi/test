<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Tableau Blanc - {{ whiteboard.title }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
            color: #333;
        }
        h1 {
            color: #1a1d2d;
            border-bottom: 3px solid #fbbf24;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .info {
            background: #f3f4f6;
            padding: 15px;
            margin: 20px 0;
            border-left: 4px solid #3b82f6;
        }
        .info p {
            margin: 5px 0;
        }
        .canvas-container {
            margin: 30px 0;
            text-align: center;
        }
        .canvas-container img {
            max-width: 100%;
            border: 2px solid #e5e7eb;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        .footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #e5e7eb;
            font-size: 12px;
            color: #6b7280;
            text-align: center;
        }
    </style>
</head>
<body>
    <h1>{{ whiteboard.title }}</h1>
    
    {% if whiteboard.description %}
        <p><strong>Description:</strong> {{ whiteboard.description }}</p>
    {% endif %}

    <div class="info">
        <p><strong>Créé par:</strong> {{ whiteboard.createdBy.nom }}</p>
        <p><strong>Date de création:</strong> {{ whiteboard.createdAt|date('d/m/Y à H:i') }}</p>
        <p><strong>Dernière modification:</strong> {{ whiteboard.updatedAt|date('d/m/Y à H:i') }}</p>
        <p><strong>Visibilité:</strong> {{ whiteboard.isPublic ? 'Public' : 'Privé' }}</p>
    </div>

    <div class="canvas-container">
        <h2>Contenu du Tableau</h2>
        <img src="{{ image_path }}" alt="Tableau blanc">
    </div>

    <div class="footer">
        <p>Document généré le {{ "now"|date('d/m/Y à H:i') }} - SmartNexusAI Collaboration Module</p>
    </div>
</body>
</html>
